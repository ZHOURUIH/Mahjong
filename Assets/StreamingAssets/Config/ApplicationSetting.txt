// 注意
// 该文件中可使用双斜杠来对单行进行注释
// 可以添加任意的换行和空格
// 变量命名应与代码中枚举命名相同

// 是否全屏显示,0为不全屏,1为全屏,2为无边框窗口,如果全屏显示,则默认为最大分辨率
AS_FULL_SCREEN = 0

// 窗口宽度
AS_WINDOW_WIDTH = 1920

// 窗口高度
AS_WINDOW_HEIGHT = 1080

// 是否锁帧
AS_LOCK_FRAME = 1

// 锁定的最大帧率
AS_MAX_FRAME = 100

// 颜色位数
AS_COLOUR_BITS = 32

// 是否将每帧的时间固定下来
AS_USE_FIXED_TIME = 0

// 每帧的固定时间,单位秒
AS_FIXED_TIME = 0.015

// 当不使用固定时间时,每帧时间的上限,单位秒
AS_MAX_FRAME_TIME = 0.15

// 垂直同步
AS_VERTICLE_SYNC = 0