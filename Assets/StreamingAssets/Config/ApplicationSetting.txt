// 注意
// 该文件中可使用双斜杠来对单行进行注释
// 可以添加任意的换行和空格
// 变量命名应与代码中枚举命名相同

// 是否全屏显示,0为不全屏,1为全屏并且使用屏幕最佳分辨率,2为无边框窗口,3为全屏并且使用配置文件中的分辨率
GDF_FULL_SCREEN = 1

// 窗口宽度,全屏时该参数无效
GDF_SCREEN_WIDTH = 3840

// 窗口高度,全屏时该参数无效
GDF_SCREEN_HEIGHT = 1080

// 屏幕自适应的方式,0为基于锚点的自适应,可以根据不同分辨率调整布局排列,1为简单拉伸,2为多屏拼接后复制显示
GDF_ADAPT_SCREEN = 0

// 显示屏数量,用于多屏横向组合为高分辨率,只能在GDF_ADAPT_SCREEN为2的情况下使用,当GDF_SCREEN_COUNT有效时,值不能小于2
GDF_SCREEN_COUNT = 1

// 是否将每帧的时间固定下来
GDF_USE_FIXED_TIME = 0

// 每帧的固定时间,单位秒
GDF_FIXED_TIME = 0.015

// 垂直同步,0为关闭垂直同步,1为开启垂直同步
GDF_VSYNC = 0